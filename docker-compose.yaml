version: '3'

services:
  webpack-dev-server:
    build: .
    command: webpack-dev-server --mode development --host 0.0.0.0
    ports:
      - 8080:8080
    volumes:
      - ./src/:/module/src/:ro
  backend-python:
    build: backends/python/
    ports:
      - 8081:80